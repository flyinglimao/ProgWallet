import { IntersectionLogic } from "../../types";

export function IntersectionLogic({ guard }: { guard: IntersectionLogic }) {
  return (
    <div className="pl-2">It requires all the following rules satisify:</div>
  );
}
